Master :
ssh ubuntu@18.217.119.87

About ReplicaSet
===========================
=> ReplicaSet ensures that a specified number of pod replicas are running at  any given time for High-Availability

=> If the Pod is Crashed then ReplicaSet brings up another Pod Automatically.

=> ReplicaSet spans across multiple nodes of Clustor 

=> ReplicaSet maintains the Pod Replicas across the Clustor.

=> ReplicaSet helps to Scale the Pods across the Cluster 

=> ReplicaSet helps to main the load across the Cluster 


kubectl explain rs
kubectl explain replicaset

========================
Replica Set Demo #1 : 
========================
YAML Configuration file for ReplicaSet : -- myreplicaset.yml
-----------------------------------------------------------------------------
apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: myjlc-replicaset
  labels:
    app: myjlc
spec:
  selector:
    matchLabels:
      app: myjlc
  replicas: 5
  template:
    metadata:
      name: myjlc-pod
      labels:
        app: myjlc
    spec:
      containers:
        - name: myjlc-container
          image: nginx
          ports:
            - containerPort: 8080


===============================
scp myreplicaset.yml ubuntu@18.217.119.87:/home/ubuntu/myrs

YAML Configuration file for Pod: -- mypod.yml
-------------------------------------------------------------
apiVersion: v1
kind: Pod
metadata:
  name: mypod1
  labels:
    app: mypods
spec:
  containers:
  - name: myconatiner1
    image: nginx
    ports:
     - containerPort: 8080


===============================
scp mypod.yml ubuntu@18.217.119.87:/home/ubuntu/myrs


kubectl create -f  myreplicaset.yml

kubectl get replicasets

kubectl get pods

kubectl get pods -o wide

kubectl get pods --show-labels

***Delete the Pod

kubectl delete pod myjlc-rs-hqsjd 

*** Create New Pod
kubectl create -f  mypod.yml


Edit mypod.yml

scp mypod.yml ubuntu@18.217.119.87:/home/ubuntu/myrs

*** Create New Pod
kubectl create -f  mypod.yml

kubectl get pods

kubectl get pods --show-labels

kubectl get pods  -l app=myjlc

kubectl get pods  -l app=mypods

kubectl get pods  -l myapp=myjlc-app

kubectl get pods --show-labels -l myapp=myjlc-app


kubectl get replicasets

kubectl describe replicaset myjlc-rs

Scale :
=====

kubectl scale replicaset myjlc-rs  --replicas=9

kubectl get pods --show-labels

kubectl scale replicaset myjlc-replicaset --replicas=3

kubectl get pods --show-labels

Delete
=====
kubectl delete replicaset myjlc-rs


================================
Can we say  
"Metadata Name" helps to name the each POD while creating PODS via Replicaset.

"LABEL" helps to prevent additional POD creation.

"LABEL" helps to pick required pods

Define ReplicaSet YML
Create ReplicaSet
Describe ReplicaSet
Scaling ReplicaSet  
Delete ReplicaSet 


